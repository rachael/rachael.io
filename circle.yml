machine:
  environment:
    MOCHA_FILE: "$CIRCLE_TEST_REPORTS/test-results.xml"

dependencies:
  pre:
    - rm -rf node_modules/rachael-io-common && npm config set registry http://registry.npmjs.org/

deployment:
  dokku:
    branch: master
    commands:
      - ./bin/deploy_dokku
